---
import { clsx } from "clsx";

export interface Props {
    as?: "section" | "div";
    class?: string;
}

const { as: Element = "section", class: className } = Astro.props;
---

<Element class={clsx("md:mx-8 max-w-8xl", className)}>
    <slot />
</Element>